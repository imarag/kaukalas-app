{
  "name": "kaukalas-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add . && git commit -m \"Committing...\" && git push && git subtree push --prefix dist origin gh-pages"  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.4",
    "daisyui": "^5.0.19"
  },
  "devDependencies": {
    "vite": "^6.3.2"
  }
}
