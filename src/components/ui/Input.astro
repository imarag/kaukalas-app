---
const {
  type = "text",
  errorMessage = "error",
  class: className = "",
  ...attrs
} = Astro.props;
---

<input
  type={type}
  class={`input validator block w-full bg-white text-black ${className}`}
  {...attrs}
/>
<div class="m-0 validator-hint">{errorMessage}</div>
