---
const { text, class: className = "" } = Astro.props;
const titleClass = `text-lg lg:text-xl text-center lg:max-w-3xl mx-auto font-extralight ${className}`;
---

<p class={titleClass}>
  {text}
</p>
