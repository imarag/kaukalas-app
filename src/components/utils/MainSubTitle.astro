---
const { text, class: className = "" } = Astro.props;
const globalClass = `text-lg lg:text-xl text-center lg:max-w-3xl mx-auto font-extralight ${className}`;
---

<p class={globalClass}>
  {text}
</p>
