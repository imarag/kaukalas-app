---
const { text, class: className = "" } = Astro.props;
const globalClass = `text-4xl lg:text-5xl font-semibold text-primary text-center mb-4 ${className}`;
---

<h2 class={globalClass}>
  {text}
</h2>
